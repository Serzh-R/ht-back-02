import { emailManager } from '../managers/EmailManager'

export const emailService = {
  async doOperation(user: any): Promise<any> {
    // save to repo
    // get user from repo
    //await emailManager.sendEmailRecoveryMessage({ user })
  },
}
